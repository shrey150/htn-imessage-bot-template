# Reference only. Use a secret manager or source scripts/keys.sh.
# Terminal / headless mode:
BROWSERBASE_API_KEY=
AI_GATEWAY_API_KEY=
# Optional portable Linq mode (Vercel Connect manages its own credentials):
LINQ_API_KEY=
LINQ_WEBHOOK_SECRET=
LINQ_ALLOWED_SENDERS=
